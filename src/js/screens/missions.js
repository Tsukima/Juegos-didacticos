import {allMissions} from '../missions/mission-engine.js';import {store} from '../core/store.js';
const names={word:'Palabras',phrase:'Frases',comprehension:'Comprensión',action:'Acción'};
export function missionsScreen(){const s=store.get();return `<p class="eyebrow">Mapa de aventuras</p><h1>Misiones</h1><p class="muted">Elige lo que te apetezca. Puedes repetir cualquier reto.</p><div class="section grid">${Object.entries(names).map(([type,name])=>{const list=allMissions.filter(m=>m.type===type),done=list.filter(m=>s.completed.includes(m.id)).length;return `<article class="card mission-card"><span class="pill">${list[0].duration}–3 min</span><h2>${name}</h2><p>${done} de ${list.length} completadas</p><div class="progress"><span style="width:${done/list.length*100}%"></span></div><a class="button" href="#lectura?tipo=${type}">Explorar</a></article>`}).join('')}</div>`}
