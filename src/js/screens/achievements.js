import {store} from '../core/store.js';
import {puzzleMarkup} from '../characters/companions.js';
const badges=[['🌱','Primer paso',1],['📚','Lector curioso',5],['🔥','En racha',10],['🧩','Mente detective',20],['🛡️','Escudo digital',30],['💛','Buen compañero',45],['🏗️','Constructor de frases',60],['🏆','Gran explorador',75]];
export function achievementsScreen(){const s=store.get();return `<p class="eyebrow">Cada esfuerzo cuenta</p><h1>Logros</h1><section class="stat-row"><article class="card stat"><strong>${s.stars}</strong>Estrellas</article><article class="card stat"><strong>${s.xp}</strong>Puntos</article><article class="card stat"><strong>${s.streak}</strong>Días de racha</article></section><section class="section">${puzzleMarkup(s)}</section><section class="section badge-grid">${badges.map(([icon,name,n])=>`<article class="card badge ${s.completed.length<n?'locked':''}"><div class="badge-icon">${icon}</div><h3>${name}</h3><p class="muted">${s.completed.length>=n?'Conseguido':`${n} misiones`}</p></article>`).join('')}</section><article class="section card accent"><h2>Tu esfuerzo ya es un logro</h2><p>No comparamos tu camino con el de nadie. Una palabra leída, una pausa pedida o un nuevo intento son victorias reales.</p></article>`}
