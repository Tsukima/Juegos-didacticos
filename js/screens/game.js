import {getMission} from '../missions/mission-engine.js';import {characters,characterCard} from '../characters/characters.js';import {store} from '../core/store.js';import {shuffle,speak,toast} from '../core/utils.js';
const done=(m,feedback)=>{const fresh=store.complete(m.id,m.type);feedback.className='feedback success';feedback.innerHTML=`<strong>¡Misión cumplida!</strong> ${characters[m.guide].message} ${fresh?'Ganaste una estrella.':'Esta práctica también cuenta.'}<div class="actions" style="margin-top:.8rem"><a class="button" href="#misiones">Siguiente aventura</a><a class="button secondary" href="#logros">Ver logros</a></div>`;toast('⭐ Tu esfuerzo suma')};
export function gameScreen(id){const m=getMission(id);if(!m)return '<div class="empty"><h1>Misión no encontrada</h1><a class="button" href="#misiones">Volver</a></div>';setTimeout(()=>bindGame(m),0);let body='';if(m.type==='word')body=`<div class="game-prompt">${m.data.word}</div><p>Sepárala en sílabas: <strong>${m.data.syllables}</strong></p><button class="button secondary" data-speak="${m.data.word}">🔊 Escuchar</button><button class="button complete">La he leído</button>`;if(m.type==='phrase'){const words=m.data.text.replace('.','').split(' ');body=`<p>Escucha, lee y después ordena la frase.</p><button class="button secondary" data-speak="${m.data.text}">🔊 Escuchar</button><div class="sentence-zone" aria-label="Tu frase"></div><div class="word-bank">${shuffle(words).map(w=>`<button class="word-chip">${w}</button>`).join('')}</div><button class="button check-order" data-answer="${m.data.text.replace('.','')}">Comprobar</button>`}if(m.type==='comprehension')body=`<div class="game-prompt">${m.data.text}</div><h2>${m.data.question}</h2><div class="options">${m.data.options.map((o,i)=>`<button class="option answer" data-i="${i}">${o}</button>`).join('')}</div>`;if(m.type==='action')body=`<div class="game-prompt">${m.data.icon} ${m.data.text}</div><p>Hazlo a tu ritmo. Si hoy no quieres moverte, puedes leerlo en voz alta.</p><button class="button complete">¡Hecho!</button>`;return `<div class="game-shell"><a href="#lectura?tipo=${m.type}" class="muted">← Salir con calma</a><p class="eyebrow" style="margin-top:1.5rem">${m.duration} minutos · ${m.reward}</p><h1>${m.title}</h1><article class="card">${characterCard(m.guide)}<hr style="border:0;border-top:1px solid var(--line);margin:1rem 0">${body}<div class="feedback" hidden></div></article></div>`}
function bindGame(m){const root=document.querySelector('.game-shell'),fb=root?.querySelector('.feedback');if(!root)return;root.querySelectorAll('[data-speak]').forEach(b=>b.onclick=()=>speak(b.dataset.speak));root.querySelector('.complete')?.addEventListener('click',()=>{fb.hidden=false;done(m,fb)});root.querySelectorAll('.answer').forEach(b=>b.onclick=()=>{fb.hidden=false;if(+b.dataset.i===m.data.answer)done(m,fb);else{fb.className='feedback try';fb.innerHTML='<strong>Buen intento.</strong> Vuelve al texto y busca una pista. No pierdes nada.'}});const zone=root.querySelector('.sentence-zone'),bank=root.querySelector('.word-bank');bank?.addEventListener('click',e=>{if(e.target.matches('.word-chip'))zone.append(e.target)});zone?.addEventListener('click',e=>{if(e.target.matches('.word-chip'))bank.append(e.target)});root.querySelector('.check-order')?.addEventListener('click',e=>{fb.hidden=false;const value=[...zone.children].map(x=>x.textContent).join(' ');if(value.toLowerCase()===e.target.dataset.answer.toLowerCase())done(m,fb);else{fb.className='feedback try';fb.innerHTML='<strong>Casi.</strong> Escucha otra vez y mueve las palabras. Puedes intentarlo todas las veces que necesites.'}})}
