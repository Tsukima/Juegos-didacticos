import {securityScenarios,safetySteps} from '../security/security-data.js';import {store} from '../core/store.js';
export function securityScreen(){const s=store.get();return `<p class="eyebrow">Academia del escudo</p><h1>Seguridad digital</h1><section class="card accent character"><div class="avatar">🍕</div><div><h2>Mr. Pizza te reta</h2><p>«He escondido trampas en mensajes y webs. Si paras, piensas y pides ayuda, no podré engañarte».</p></div></section><section class="section"><h2>El plan de 5 pasos</h2><div class="grid">${safetySteps.map((x,i)=>`<article class="card"><span class="badge-icon">${i+1}</span><strong>${x}</strong></article>`).join('')}</div></section><section class="section"><h2>Detecta las trampas</h2><div class="grid">${securityScenarios.map(x=>`<article class="card mission-card"><div class="mission-meta"><span class="pill">3 min</span>${s.completed.includes(x.id)?'<span class="pill warm">✓ Resuelta</span>':''}</div><h3>${x.title}</h3><p>${x.text}</p><a class="button" href="#reto-seguro/${x.id}">Resolver</a></article>`).join('')}</div></section><article class="section card adult-note"><strong>No estás solo.</strong> Si algo te asusta, te incomoda o te pide guardar un secreto, cierra la conversación y cuéntaselo a un adulto de confianza. No es culpa tuya.</article>`}
